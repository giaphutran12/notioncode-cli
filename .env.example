NOTION_TOKEN=
NOTION_DATABASE_ID=
# Optional: local webhook path for Notion comment.created events (default: /webhook)
# NOTIONCODE_WEBHOOK_PATH=/webhook
# Optional: override Notion API base URL for local mocking/harnesses
# NOTION_API_BASE_URL=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
# Optional: override the fallback chain with openai, anthropic, or gemini.
# AGENT_PROVIDER=
# Optional: claude supports Anthropic only; opencode supports all configured providers.
# AGENT_RUNNER=
TARGET_REPO_PATH=
