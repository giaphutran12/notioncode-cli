# Demo sample tickets

This file is meant for quick copy and paste into Notion. Live Notion creation is out of scope for this shell session, so use the content below to seed the demo database fast.

## 1. Add a /health endpoint

- Name: Add a /health endpoint
- Status: Not started
- Description: Add a simple health check endpoint that returns OK so the service can be checked during deployment and demos.

## 2. Fix the login validation bug

- Name: Fix the login validation bug
- Status: Not started
- Description: Fix the validation logic so invalid login input is rejected correctly and the user sees a clear error state.

## 3. Add dark mode toggle

- Name: Add dark mode toggle
- Status: Not started
- Description: Add a dark mode toggle that updates the UI theme and makes the app feel more polished in the demo.
